{
  "_comment": "Copy to env/production.json and fill in from the production Supabase project (Settings -> API). The filled-in file is gitignored and must never be committed (D34). Use the anon / publishable key only - never service_role (D37).",
  "ARENA_ENV": "production",
  "SUPABASE_URL": "https://REPLACE_ME.supabase.co",
  "SUPABASE_ANON_KEY": "REPLACE_ME"
}
